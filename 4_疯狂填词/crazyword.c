#include <stdio.h>
#include <string.h>
int main(int argc, char *argv[] )
{
    char n;
    char v;
    char adv;
    char adj;

    pritnf("Enter a noun: "); scanf("%s",&n);
    pritnf("Enter a verb: "); scanf("%s",&v);
    pritnf("Enter a adjective: "); scanf("%s",&adj);
    pritnf("Enter a adverb: "); scanf("%s",&adv);

    //创建故事
}